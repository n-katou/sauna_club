  <% comments.each do |comment| %>
    <div class="border rounded">
      <br>
      <div class="row">
        <div class="col-1"></div>
        <div class="col-2">
          <p>コメント投稿者</p>
          <%= image_tag comment.customer.get_profile_image(100,100) %>
        </div>
        <div class="col-1"></div>
        <div class="col-4 d-flex align-items-center">
          <%= comment.comment_content %>
        </div>
        <div class="col-3">
          投稿日  <i class="fa-solid fa-calendar"></i> <%= comment.created_at.strftime('%Y%m/%d') %>
        </div>
      </div>

      <div class="row">
        <div class="col-1"></div>
        <div class="col-3">
          <%= link_to customer_path(comment.customer.id) do %>
            <%= comment.customer.account_name %>
          <% end %>
        </div>
        <div class="col-4"></div>
        <div class="col-4">
          <% if comment.customer == current_customer %>
            <div class="row">
              <div>
                <%= link_to "編集する", edit_post_comment_path(comment.post.id, comment.id),class: "text-white bg-primary btn"%>
              </div>
              <div class="ml-4">
              <!--mlはマージンレフトのこと-->
                <form action= "/posts/<%= @post.id %>/comments/<%= comment.id %>" accept-charset="UTF-8" method="post"><input type="hidden" name="_method" value="delete" autocomplete="off">
                  <input type="hidden" name="authenticity_token" value="" autocomplete="off">
                  <input type="submit" name="commit" value="削除する" class="btn btn-danger" data-confirm="本当に削除しますか？" data-disable-with="削除する">
                </form>
              </div>
            </div>
          <% end %>
        </div>
      </div>
      <br>
    </div>
  <% end %>

  <div class="row">
    <div class="col text-center"><%= paginate comments %></div>
  </div>

   <div class="row">
    <h3>コメントする</h3>
  </div>
  <br><br>

  <div id="comment">
    <%= form_with(model: [@post,@comment],local: false,url:  post_comments_path(@post)) do |f| %>
    <!--JSを使う場合local: falseになる-->
    <% if f.object.errors.present? %>
      <div id="error_explanation">
        <h2><%= f.object.errors.count %>件のエラーが発生しました。</h2>
        <ul>
          <% f.object.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
    <div class= "row">
      <div class="col-4"></div>
      <div class="col-6">
        <h3><%= f.text_area :comment_content,rows: "5", placeholder: "コメントをここに" %></h3>
      </div>
    </div>

    <div class="row">
      <div class="col-6"></div>
      <div class="col-3">
        <%= f.submit "投稿する",class: "text-white bg-primary btn" %>
      </div>
    </div>
  <% end %>
  </div>

